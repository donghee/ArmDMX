#include "CppUTest/TestHarness.h"
#include "CppUTestExt/MockSupport.h"

extern "C"
{
#include "switch.h"
}

TEST_GROUP(ROTARY_SWITCH)
{
	void setup()
    {
    }

    void teardown()
    {
    }
};

TEST(ROTARY_SWITCH, test_Channel_Select)
{
    get_channel(0,ON,ON,0);
	LONGS_EQUAL(0,0);
}
